# DESIGN.md — PEPE / DOGE 突破雷达

> 本文件沉淀对本产品的设计思考，用于约束 UI 走向，避免退化成"科技蓝 + 圆角卡片 + 渐变"的通用后台模板。

## 一、产品灵魂与意象锚点

这不是一个交易所，也不是喊单面板，而是一座**深夜港口上的雷达观测站**。

物理意象：瞭望塔顶，一面缓慢旋转的雷达屏，磷光扫描线扫过海面（市场），寻找的是"风暴前的平静"——波动收缩、成交量降温、能量积蓄，以及风眼形成前一瞬的异动。

我们希望用户第一眼感受到的是：**冷静、克制、可解释的态势报告**，而不是催促下单的情绪。

一句话气质：**「先给状态，再给证据，最后说还缺什么。」**

## 二、视觉策略

- **深空底色**：不是纯黑，而是带冷蓝的深色（近 `#0B0E14`），让磷光与琥珀色能在暗面衬托下"亮"起来。
- **品牌印记**：顶部总状态区放置一圈缓慢的雷达扫描脉冲（低频呼吸感），作为整站的识别符号；六个状态用"状态环/状态边徽"表达。
- **图形语言**：信号强度条、状态六边疆环、K 线为主体视觉；数值一律等宽排列，营造仪表读数感。
- **摄影/图像方向**：以线、点、光斑为主，弱化大面积色块，避免"贴纸式卡片"。

## 三、配色方案

| 角色 | 色值（约） | 意象来源 |
|---|---|---|
| 背景 | `#0A0E15` | 深夜海面与天空交接的深蓝黑 |
| 表面/卡片 | `#111722` | 雷达屏磨砂玻璃面板 |
| 主强调·磷光青绿 | `#37E6A6` | 雷达扫描线的荧光；用于"确认/激活/PEPE" |
| 琥珀金 | `#F5B544` | 狗狗毛色 + 警示刻度；用于"临界/DOGE/过热" |
| 冷蓝白 | `#8AB4F8` | 中性数据、BTC、链接 |
| 信号红 | `#FF5D5D` | 硬否决 / 失效位，限量使用 |
| 中性灰 | `#64748B` | 次级文本、缺失/禁用 |

六状态色映射（颜色必须配合图标 + 文字，不能只靠颜色区分）：
- 禁止关注：灰 `#64748B`
- 蓄势观察：蓝灰 `#7CA3C9`
- 临界突破：琥珀 `#F5B544`
- 突破确认：磷光青绿 `#37E6A6`
- 等待回踩：青 `#4FC3F7`
- 信号失效：红 `#FF5D5D`

## 四、字体排版

- 中文：`Noto Sans SC`（兜底 `PingFang SC` / `Microsoft YaHei`）——正文、标题、状态说明。
- 数字/代码：`JetBrains Mono`（兜底 `SFMono` / `Consolas`），统一 `font-variant-numeric: tabular-nums`，形成仪表读数感。
- 节奏：顶部行动结论用大字号短句（一句话说清"现在是什么状态"）；证据区用小字号分条的"条件清单"，一条一行；数据陈旧/缺失用小标签与图标标明，不埋在正文里。

## 五、动效与交互

- 运动性格：**低频呼吸**，不跳、不弹、不炫技。
- 雷达扫描脉冲约 2.5s 一圈，缓动 `ease-in-out`，作为氛围背景而非功能动效。
- 状态/数字更新用 200ms 淡入；筛选器、周期切换即时响应。
- 统一缓动 `cubic-bezier(0.4, 0, 0.2, 1)`；尊重 `prefers-reduced-motion`。

## 六、设计禁忌

- ❌ 不要"科技蓝 + 圆角卡片 + 蓝紫渐变"这类万能组合。
- ❌ 不要做成交易所复刻（订单簿、深度图、跳动的报价带堆叠）。
- ❌ 不要只用红/绿表达涨跌或好坏，必须配合文字与图标。
- ❌ 不要大圆角糖果卡片、玻璃拟态的过度发光。
- ❌ 不要过度动画、连续弹跳、无限缩放等干扰阅读的效果。
- ✅ 记住：每个状态都要能被"遮住颜色后仍看得懂"。